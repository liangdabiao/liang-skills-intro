# 把任意图片变成"照着拼"的拼豆图纸 —— pindou-pattern 介绍

你有没有过这种经历：看到一张可爱的卡通头像，想用拼豆（Perler Bead）把它做出来，可一想到要一格一格去数颜色、算坐标，就头大？

**pindou-pattern 就是来解决这件事的。**

它把一个图片（或像素数据）自动变成一张可以直接照着拼的"拼豆施工图"——深色标题栏、四周标着行列坐标、每个小格子里写着对应的品牌色号、每 10 格加重一条线，最后还附上一份"每种颜色用了多少颗"的物料清单。

一句话总结：**你给它一张图，它还你一张能直接上手拼的图纸。**

---

## 它到底能做什么

- **图片变图纸**：上传一张 PNG / JPG / WebP 等格式的图片，指定网格大小（比如 32×32），它就自动量化成拼豆格子，输出一张带色号、坐标、网格线的 PNG。
- **五大品牌色号**：内置 MARD、COCO、漫漫、盼盼、咪小窝共 205 种颜色，生成的色号都是你**真能买到的豆子**，不会出现"图上有、店里没有"的尴尬。
- **物料清单（CSV）**：除了图纸，还能导出"每种颜色用了多少颗、总共多少颗"，方便你按单买料，不浪费。
- **AI 优化**：如果你对图纸效果不满意（太乱、太杂、细节糊了），它会先用 AI 把原图改绘成干净的像素风，再重新生成图纸。
- **白底处理**：卡通角色这类白底图，可以设置"近白色当作空格"，不把豆子浪费在背景上。

---

## 一个具体例子

假设你想拼一只卡通猫。你只需要准备一张卡通猫的图片，运行一行命令：

```bash
node .agents/skills/pindou-pattern/scripts/generate.js 素材/卡通猫.png
```

默认就会生成一张 32×32 的 MARD 品牌图纸（`卡通猫_pattern.png`），终端还会清楚地告诉你：画布多大、用了几种颜色、总共多少颗豆、每种色号各用了多少。

想要更大更精细？加个 `--size 48`。想换品牌？`--system 盼盼`。想要物料清单？`--csv 物料.csv`。

如果不满意第一次的效果，也可以先让 AI 把图片改绘成干净的像素风，再重新出图——尤其是照片、AI 生成的图，先优化再量化，出来的图纸会清爽很多。

---

## 谁适合用

- 拼豆爱好者，想把自己喜欢的图变成可拼的图纸；
- 想做礼物、摆件、钥匙扣，但懒得手动数格子的手作党；
- 需要批量出图（比如一整套角色）的创作者。

---

## 一点说明

图纸是**确定性生成**的，规格（尺寸、颜色数、用豆量）都以脚本输出的数字为准，不需要靠"肉眼判断好不好看"——好不好拼，你自己看图纸就能判断。

另外，它只负责"出图"，不替你做视觉审美判断：图纸对不对、颜色分布合不合适，最终还是你的眼睛说了算。

---

> 这是一篇用于演示风格的"测试稿"。正式撰写全部项目介绍时，会统一格式、补充每个项目的触发场景与适用边界，并按你的要求决定是否配图。
